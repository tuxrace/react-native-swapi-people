import React, {memo} from 'react';
import {
  SafeAreaView,
  StyleSheet,
  ScrollView,
  View,
  Text,
  StatusBar,
} from 'react-native';

const Main = () => {
  return (
    <View>
      <Text> People Finder </Text>
    </View>
  );
};

export default memo(Main);
